Simple communication application based on UDP protocol.
User an set the max size of packet, message is then splitted
into fragments, which are sent as separate packets.

Usage:
    $ python server-client.py